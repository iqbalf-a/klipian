# Salin berkas ini jadi .env, lalu isi nilainya. .env TIDAK ikut commit
# (lihat .gitignore) -- berkas contoh ini yang ikut commit, tanpa nilai asli.

# Token HuggingFace (read-only cukup), dipakai SEKALI untuk mengunduh bobot
# model speaker-diarization-community-1 (fitur AI Framing). Buat token di:
# https://huggingface.co/settings/tokens
# Modelnya gated -- setujui akses juga di halaman modelnya sebelum token bisa
# dipakai mengunduh:
# https://huggingface.co/pyannote/speaker-diarization-community-1
HF_TOKEN=
