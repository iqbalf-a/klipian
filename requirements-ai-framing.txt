# klipian -- dependensi TAMBAHAN untuk AI Framing (deteksi pembicara otomatis)
#
# Opsional: hanya perlu diinstal kalau memakai tombol "AI Framing" di layar
# Framing. Menarik PyTorch (~ratusan MB) -- itu sebabnya dipisah dari
# requirements.txt utama, bukan digabung begitu saja.
#
# Butuh HF_TOKEN sekali di awal untuk mengunduh bobot modelnya -- lihat
# .env.example dan komentar di klipian/diarize.py.
#
#   pip install -r requirements.txt -r requirements-ai-framing.txt

pyannote.audio>=4.0.0      # speaker diarization (CPU, lewat torch/torchaudio)
soundfile>=0.12.0          # baca WAV manual -- workaround bug torchcodec di Windows
# deteksi wajah (Haar cascade, BUKAN PyTorch) -- merapatkan kotak framing.
# DIKUNCI di bawah 5.0: versi 5.x tidak lagi menyertakan berkas cascade-nya
# sama sekali (perubahan packaging upstream) -- cv2.data.haarcascades jadi
# folder kosong dan _load_detector() di facebox.py gagal memuat apa pun.
opencv-python-headless>=4.9.0,<5.0.0
